#include "bits/stdc++.h"
using namespace std;


int main () {
    // try to remember this it helps in optimizing the 
    // input/output time and makes code faster 
    // have this at the top of main function
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    
}